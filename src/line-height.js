import { css } from 'glamor';

/*
  LINE HEIGHT
*/
const rules = {
  lh0: css({  lineHeight: 0 }),
  lhs: css({  lineHeight: 0.8 }),
  lh: css({   lineHeight: 1 }),
  lhm: css({  lineHeight: 1.25 }),
  lhl: css( { lineHeight: 1.6 }),
};

export default rules;
