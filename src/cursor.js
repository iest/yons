import { css } from 'glamor';

/*
  CURSOR
*/

const rules = {
  cp: css({ cursor: 'pointer' }),
  cd: css({ cursor: 'default' }),
  ct: css({ cursor: 'text' }),
  cna: css({ cursor: 'not-allowed' }),
};

export default rules;
