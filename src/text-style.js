import { css } from 'glamor';

/*
  TEXT STYLES
*/

const rules = {
  ttc: css({ textTransform: 'capitalize' }),
  ttl: css({ textTransform: 'lowercase' }),
  ttu: css({ textTransform: 'uppercase' }),
  ttn: css({ textTransform: 'none' }),
};

export default rules;
