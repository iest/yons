import { css } from 'glamor';

/*
  FONT WEIGHTS
*/
const rules = {
  n: css({        fontWeight: 'normal' }),
  b: css({        fontWeight: 'bold' }),
  semibold: css({ fontWeight: '500' }),
  l: css({        fontWeight: 'lighter' }),

  bold: css({ fontWeight: 'bold' }),
};

export default rules;
