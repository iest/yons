import { css } from 'glamor';

/*
  TRANSFORMS
*/

const rules = {
  rotate90: css({  transform: 'rotate(90deg)' }),
  rotate180: css({ transform: 'rotate(180deg)' }),
  rotate270: css({ transform: 'rotate(270deg)' }),
};

export default rules;
