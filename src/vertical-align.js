import { css } from 'glamor';

/*
  VERTICAL ALIGN
*/

export const rules = {
  va:  css({ verticalAlign: 'baseline' }),
  vat: css({ verticalAlign: 'top' }),
  vam: css({ verticalAlign: 'middle' }),
  vab: css({ verticalAlign: 'bottom' }),
};

export default rules;
